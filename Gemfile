source 'https://rubygems.org'

# Use the GitHub Pages dependency set for compatibility
gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
end
